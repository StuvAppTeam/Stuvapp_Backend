<p id="notice"><%= notice %></p>

<h1>Listing Mages</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Path</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @images.each do |mage| %>
      <tr>
        <td><%= mage.title %></td>
        <td><%= mage.path %></td>
        <td><%= link_to 'Show', mage %></td>
        <td><%= link_to 'Edit', edit_mage_path(mage) %></td>
        <td><%= link_to 'Destroy', mage, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Image', new_mage_path %>
